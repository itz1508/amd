AMD Track 1 remote provider validation

Harness contract:
  FIREWORKS_API_KEY is injected by the evaluator.
  FIREWORKS_BASE_URL is injected by the evaluator.
  ALLOWED_MODELS is injected by the evaluator as exact comma-separated model IDs.
  No submitted default URL, key, or model ID is permitted.

Environment presence:
  MODEL_ACCESS_KEY present=false
  FIREWORKS_API_KEY present=true

Exploratory direct probe, not evaluator proof:
  endpoint=https://inference.do-ai.run/v1/chat/completions
  model=gpt-4o from a user-provided example
  credential used=existing local FIREWORKS_API_KEY value
  HTTP status=401 Unauthorized
  exit code=1

Interpretation:
  This 401 does not evaluate the submission harness contract. The submitted
  runtime must not use this example endpoint, model, or MODEL_ACCESS_KEY mapping.
  No credential value was logged. Local remote-only and mixed validation remain
  unverified because the evaluator-provided FIREWORKS_BASE_URL and
  ALLOWED_MODELS are not available in the current shell.
