{
  "capability_id": "code_generation",
  "version": "1.0.0",
  "task_categories": ["code_generation"],
  "description": "Generates code for bounded tasks.",
  "execution_modes": ["deterministic_tool", "local_model", "remote_model"],
  "allowed_tools": ["code_generator"],
  "allowed_validators": ["code_checker"],
  "allowed_models": [],
  "input_schema": "schemas/task-input.schema.json",
  "output_schema": "schemas/task-result.schema.json",
  "benchmark_suites": ["generation-core"],
  "deterministic_eligible": true,
  "supports_batch": true,
  "timeout_ms": 10000,
  "max_retries": 1,
  "owner_module": "amd_track1.capabilities.executors",
  "implementation": "amd_track1.capabilities.executors",
  "status": "active"
}
